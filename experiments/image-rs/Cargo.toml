[package]
name = "image-rs-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
image = { version = "0.25", default-features = false, features = ["png", "jpeg", "gif"] }
wasm-bindgen = "0.2"

[profile.release]
opt-level = 3
lto = true
